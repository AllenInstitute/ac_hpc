profiles {
    standard {
        process {
            executor = 'local'
            container = 'russtorres/asap-full:latest'
            containerOptions = '--volume /allen:/allen'
            beforeScript = 'chmod o+rw .'
        }
    }

    nodocker {
        process {
            executor = 'local'
        }
    }
    
   hpc {
        apptainer {
            enabled = true
            cacheDir = '/allen/programs/celltypes/workgroups/em-connectomics/laughla/Slurm/Singularity_Images/'
            autoMounts = false
            runOptions = '--bind /allen:/allen  --no-mount bind-paths'
            apptainer.pullTimeout = '2h'
        }

        process {
            maxForks = 40

            withName: createContainer {
                container = 'docker://conochur/segment:gunpowder'
                executor = "slurm"
                cpus = '10'
                time = '70:00:00'
                queue = 'emconnectome'
                clusterOptions = '--exclusive'
                memory = '30GB'
            }
        }
    }
}
